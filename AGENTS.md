# Quantum Study Workspace Instructions

## 프로젝트 목적

- 이 작업 공간은 양자컴퓨팅과 Qiskit을 체계적으로 다시 공부하기 위한 학습 공간이다.
- 학습자는 예전에 양자컴퓨팅을 공부했지만 현재 기억이 많이 흐려진 상태로 가정한다.
- 진도를 빠르게 나가는 것보다 개념 복구, 능동 회상, 손계산, 실습, 반복 복습을 우선한다.
- 기본 학습 시간은 하루 2시간이다.
- 기존에 작성된 주차별·일자별 커리큘럼과 학습 문서를 학습 진행의 기준으로 사용한다.

## 대화 원칙

- 사용자에게는 한국어 존댓말로 답변한다.
- 설명은 사용자의 현재 진도와 이해 수준에 맞춘다.
- 사용자가 “n주차 n요일 학습하자”라고 말하면 해당 날짜의 문서를 찾아 그 내용을 기준으로 수업을 시작한다.
- 수업 내용을 한꺼번에 전부 제시하지 않고, 개념 확인 → 설명 → 예측 → 손계산 → 코드 실습 → 결과 해석 → 복습 순서로 천천히 진행한다.
- 중요한 단계에서는 사용자가 직접 생각하거나 답할 기회를 먼저 제공한다.
- 사용자의 답이 틀렸을 때는 정답만 제시하지 않고 어디서 사고가 어긋났는지 설명한다.
- 이전 학습 내용과 현재 내용이 어떻게 연결되는지 지속적으로 알려준다.

## 연관 용어 설명

- 사용자가 개념이나 코드에 대한 설명을 요청하면, 그 설명을 이해하는 데 필요한 연관 용어도 함께 설명한다.
- 처음 등장하는 전문 용어는 최소한 다음 내용을 포함해 짧게라도 설명한다.
  1. 용어의 뜻
  2. 현재 주제에서 필요한 이유
  3. 다른 개념과의 관계
- 사용자가 이미 알고 있는 수준의 용어는 장황하게 반복하지 않는다.
- 사용자가 어려워할 가능성이 있는 수학·물리·Qiskit 용어는 생략하지 않는다.
- 설명 수준이 불분명하면 현재 주차와 이전 학습 기록을 기준으로 판단한다.
- 핵심 선수 지식이 부족한 경우에는 진도를 강행하지 않고 필요한 개념을 먼저 복구한다.

## 일자별 학습 문서 품질

- 모든 일자별 학습 문서는 최소 500행 이상으로 작성한다.
- 빈 줄, 같은 문장의 반복, 형식적인 체크리스트로 행 수만 채우지 않는다.
- 첫 수업과 마지막 수업 사이에 설명의 밀도와 품질 차이가 생기지 않게 한다.
- 문서는 짧은 요약본이 아니라 해당 문서만 다시 읽어도 개념을 기억하거나 처음부터 공부할 수 있는 독립적인 학습 자료여야 한다.
- 각 날짜의 주제에 맞는 구체적인 설명과 예제를 작성하고, 공통 템플릿을 내용만 바꾸지 않은 채 반복하지 않는다.
- 수식이나 코드는 결과만 제시하지 않고 왜 그렇게 되는지 중간 과정을 설명한다.
- 코드 실행 전 예상 결과와 실행 후 해석을 모두 포함한다.
- 연습문제에는 힌트뿐 아니라 사고 과정이 포함된 상세 해설을 제공한다.
- 오개념, 자주 발생하는 오류, 디버깅 방법을 해당 주제에 맞게 작성한다.

## 일자별 문서 기본 구성

각 일자별 문서는 필요한 경우 다음 구조를 포함한다.

1. 오늘의 학습 목표
2. 필요한 선수 지식과 연관 용어
3. 120분 학습 일정
4. 이전 내용 능동 회상
5. 개념의 직관적 설명
6. 수학적 정의와 단계별 유도
7. 손으로 따라가는 계산 예제
8. Qiskit 전체 실행 코드
9. 실행 전 결과 예측
10. 실행 결과와 객체 구조 해석
11. 단계별 연습문제
12. 힌트와 상세 풀이
13. 자주 발생하는 오개념
14. 오류 진단과 복구 절차
15. 완료 기준
16. 다음 수업과의 연결
17. 간격 복습 항목

## Qiskit 및 자료 사용 원칙

- `QuantumComputingKorea/Qiskit-Dev-Cert-lectures` 저장소는 개념과 과거 자격시험 범위를 참고하는 자료로 사용한다.
- 해당 저장소의 오래된 Qiskit API를 현재 코드에 그대로 복사하지 않는다.
- Qiskit API, 패키지 구조, Runtime 사용법처럼 변경될 수 있는 내용은 현재 공식 문서에서 확인한다.
- 사용하는 Python, Qiskit, Qiskit Aer, Qiskit IBM Runtime 버전을 실습 문서에 기록한다.
- 제거된 `execute`, `BasicAer`, 구형 IBMQ provider, Primitive V1 API는 현재 목적에 맞는 방식으로 변환한다.
- 상태벡터 계산, 유한 샷 sampling, 기대값 계산, noisy simulation, 실제 QPU 실행을 서로 다른 작업으로 구분한다.
- 측정 결과를 해석할 때 Qiskit의 큐비트 순서, 고전 비트 순서, 레지스터 이름을 명시적으로 확인한다.
- 실제 QPU 작업은 비용, 쿼터, 대기시간이 발생할 수 있으므로 사용자의 명시적인 확인 없이 제출하지 않는다.
- 불확실한 API나 실행 결과를 추측해서 문서에 넣지 않는다.

## 파일 변경 절차

- 파일을 만들거나 수정하기 전에 변경 목적과 영향 범위를 짧게 설명한다.
- 변경 전 상태와 변경 후 예상 상태를 사용자에게 보여준다.
- 사용자가 허용한 범위만 수정한다.
- 기존 문서를 수정할 때 사용자가 작성한 내용과 관련 없는 변경을 보존한다.
- 불확실한 요구사항이 결과를 크게 바꿀 때는 사용자에게 확인한다.
- 단순히 파일 수와 행 수만 확인하지 않고 실제 내용의 품질도 점검한다.

## 학습 문서 검증

문서 변경 후에는 다음을 확인한다.

- 모든 일자별 문서가 500행 이상인지 확인한다.
- 빈 줄이나 반복 문장으로 분량을 채우지 않았는지 표본 검토한다.
- 초반·중반·후반 문서의 설명 밀도를 비교한다.
- 코드 블록이 정상적으로 닫혀 있는지 확인한다.
- 내부 파일 링크가 유효한지 확인한다.
- 생성용 임시 문구나 치환되지 않은 변수가 남아 있지 않은지 확인한다.
- 수식, bit ordering, Qiskit 객체 접근 방식에 명백한 오류가 없는지 확인한다.
- 대표 문서를 직접 읽어 문서만으로 학습 가능한지 확인한다.

## 공개 학습 저장소 운영

- 이 프로젝트는 양자컴퓨팅 학습 과정과 실제 학습 기록을 공개하는 Git 저장소로 운영한다.
- 저장소에는 실제로 진행한 학습 내용만 기록하며, 진행하지 않은 수업을 완료한 것처럼 작성하지 않는다.
- 매일 학습이 끝나면 해당 날짜의 일일 학습 기록과 `학습_진행표.md`를 갱신한다.
- 일일 기록은 `학습기록/YYYY-MM-DD_주차_요일.md` 형식으로 작성한다.
- 수업 문서는 학습 중 오류나 부족한 설명이 발견된 경우에만 함께 수정한다.
- 사용자가 “오늘 공부한 내용 올려줘”라고 요청하면 다음 순서로 진행한다.
  1. 오늘 변경된 파일을 확인한다.
  2. 학습 기록과 진행표의 내용을 검토한다.
  3. 토큰, 비밀번호, 계정 정보, 불필요한 로컬 경로가 포함되지 않았는지 검사한다.
  4. 커밋 대상과 변경 요약을 사용자에게 보여준다.
  5. 사용자가 승인한 파일만 커밋하고 push한다.
- 커밋 메시지는 `study: YYYY-MM-DD 학습 주제` 형식을 기본으로 사용한다.
- 서로 다른 날짜의 학습 기록을 특별한 이유 없이 하나의 커밋으로 합치지 않는다.
- 학습하지 않은 문서 전체를 매일 다시 커밋하지 않는다.
- 코드 실행 결과가 크거나 다시 생성할 수 있는 파일이면 저장소에 포함하지 않는다.

## 공개 저장소 보안

- `.codex/`, `.env`, 가상환경, 캐시, 임시 파일은 커밋하지 않는다.
- IBM Quantum token, API key, 비밀번호, 인증 파일은 절대 저장소에 포함하지 않는다.
- 토큰이나 계정 정보를 코드 문자열에 직접 작성하지 않는다.
- 실제 QPU job을 기록할 때도 인증 정보와 불필요한 계정 식별 정보는 제거한다.
- 공개 전에는 staged diff와 민감정보 패턴을 모두 검사한다.
- 민감정보 가능성이 있으면 push를 중단하고 사용자에게 알린다.
- 이 공개 저장소의 Git 작성자는 `swforest <286219885+swforest@users.noreply.github.com>`를 사용한다.
- 회사·개인 이메일이 커밋 기록에 노출되지 않도록 `user.name`과 `user.email`은 이 저장소의 로컬 Git 설정으로만 지정한다.
- 전역 Git 작성자 설정은 변경하지 않는다.

## Git

- 사용자가 명시적으로 요청하기 전에는 커밋하지 않는다.
- 사용자가 명시적으로 요청하기 전에는 push하지 않는다.
- 사용자가 허용한 파일만 stage한다.
- 관련 없는 변경을 한 커밋에 섞지 않는다.
